spring:
  profiles:
    active: git
  cloud:
    config:
      server:
        git:
          url: https://github.com/dhilipk/CloudMyTrip/spring-cloud-config
          searchPaths: spring-cloud-config